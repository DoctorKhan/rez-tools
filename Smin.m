function m = Smin(mData, iDim)

% m = Smin(mData, iDim);
%
%
%
% Khan 08/01/2011

%% Default params
Default iDim = 1

%% Initialize


%% Main
m = min(mData, [], iDim);

end % Function


%% Note: type "doc hungarian" for variable prefix explanation.
