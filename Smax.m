function m = Smax(mData, iDim)

% m = Smax(mData, iDim);
%
%
%
% Khan 08/01/2011

%% Default params
Default iDim = 1

%% Initialize


%% Main
m = max(mData, [], iDim);

end % Function


%% Note: type "doc hungarian" for variable prefix explanation.
